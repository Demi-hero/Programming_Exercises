ex2 needs 

- take a user input 
- if matches user number break and declare it solved 
- if user number is less than stated number devide by a reducing fraction
- if user number is greater than stated number multiply by a reducing fraction
- use the deminising returns formula for the reducing amount (100/2^counter)
	counter starts at 1
	first number will always be 50
-make sure the user isn't lying by setting an upper and lower bound
-